const initialState = {
    jobs: [],
    loading: false,
    error: null
};

const jobReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default jobReducer;
